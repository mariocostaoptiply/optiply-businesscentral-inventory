name: Legacy release workflow disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. Use .github/workflows/CreateRelease.yaml instead."
