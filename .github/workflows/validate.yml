name: Validate AL Extension

on:
  workflow_dispatch:

jobs:
  validate:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install BcContainerHelper
        shell: pwsh
        run: Install-Module BcContainerHelper -Force -Scope CurrentUser

      - name: Build AL Project
        shell: pwsh
        run: |
          # Always install the newest BcContainerHelper (needed on GitHub runners)
          Install-Module BcContainerHelper -Force -AllowClobber
          Import-Module BcContainerHelper

          # Load credentials
          $username = "${{ secrets.BC_USERNAME }}"
          $passwordPlain = "${{ secrets.BC_PASSWORD }}"

          if (-not $username -or -not $passwordPlain) {
            throw "BC_USERNAME or BC_PASSWORD is empty — Check your GitHub Secrets."
          }

          $securePass = ConvertTo-SecureString $passwordPlain -AsPlainText -Force
          $credential = New-Object System.Management.Automation.PSCredential($username, $securePass)

          # Fetch artifact URL
          $artifactUrl = Get-BcArtifactUrl -type Sandbox -version 27.1 -country w1
          Write-Host "Using artifact: $artifactUrl"

          # Build image from artifacts (this works on GitHub runners)
          $imageName = New-BcImage `
            -artifactUrl $artifactUrl `
            -imageName "bcimage:27" `
            -includePlatform

          # Create container
          New-BcContainer -accept_eula `
            -containerName "buildcontainer" `
            -imageName "bcimage:27" `
            -auth NavUserPassword `
            -credential $credential `
            -updateHosts `
            -doNotExportObjectsToText

          # Compile the AL extension
          Compile-AppInBCContainer `
            -containerName buildcontainer `
            -appProjectFolder "$PWD/app" `
            -appOutputFolder "$PWD/output"

          Write-Host "✔ Build completed successfully"


      - name: Run AL Validation
        shell: pwsh
        run: |
          Run-AlValidation `
            -apps @("app/*.app") `
            -validateCurrent `
            -countries @("gb","w1") `
            -affixes @("hot") `
            -supportedCountries @("gb","w1")